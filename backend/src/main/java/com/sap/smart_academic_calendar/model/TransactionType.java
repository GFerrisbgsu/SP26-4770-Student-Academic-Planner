package com.sap.smart_academic_calendar.model;

/**
 * Enum representing the type of a transaction.
 * Used to distinguish between income and expense transactions.
 */
public enum TransactionType {
    EXPENSE,  // Money spent (e.g., groceries, utilities)
    INCOME    // Money received (e.g., paycheck, reimbursement, bonus)
}
